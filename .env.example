# OncoLiving - Environment Variables

# MongoDB Connection
# Get your connection string from MongoDB Atlas or use local: mongodb://127.0.0.1:27017/oncoliving
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster0
MONGODB_DB=oncoliving

# JWT Authentication
# CRITICAL: Generate a secure secret with: openssl rand -base64 64
# NEVER use the default value in production!
JWT_SECRET=your-secret-key-here

# Asaas Payment Gateway
ASAAS_API_KEY=your-asaas-api-key-here
ASAAS_ENVIRONMENT=sandbox # or 'production'

# Application Configuration
VITE_APP_ID=oncoliving-local
OWNER_OPEN_ID=owner-local

# OAuth (if using external OAuth provider)
OAUTH_SERVER_URL=http://localhost:3000
VITE_OAUTH_PORTAL_URL=http://localhost:3000

# Development Settings
VITE_DEV_LOCAL_AUTH=true

# Optional: Forge API (if using)
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# Optional: Analytics
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# Server Configuration
PORT=3000
NODE_ENV=development
